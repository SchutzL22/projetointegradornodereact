package com.example.demo.configs;

/*
 * DataSeeder — DESATIVADO.
 *
 * Este ficheiro foi desativado porque a base de dados MySQL já está
 * populada com o script definitivo.
 *
 * Para reativar: restaurar a anotação @Component e a implementação
 * de CommandLineRunner, e descomentar os imports necessários.
 *
 * Última versão funcional preservada abaixo como referência.
 */
public class DataSeeder {
    // Classe mantida como referência histórica — não é instanciada pelo Spring Boot.
}
